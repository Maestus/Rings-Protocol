#ifndef __ENTITY_H__
#define __ENTITY_H__
#include <string.h>
#include <sys/time.h>
#include <stdlib.h>
#include <stdio.h>
#endif

char * generateIdm();
char * convertIp(char *);
char * to2Bytes(char *);
char * to3Bytes(size_t);
char * to8Bytes(char *);
char * to15Bytes(char *);
